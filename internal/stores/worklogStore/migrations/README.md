The migrations directory contains all database migrations to be performed. 
To generate a new migration run

```migrate create -ext sql -dir internal/stores/worklogStore/migrations -seq MIGRATION_NAME```